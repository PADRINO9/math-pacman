name: Mobile enemy PR fix

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: write
  pull-requests: read

jobs:
  apply-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Apply mobile enemy and scale fix
        run: python tools/apply_mobile_enemy_and_scale_fix.py

      - name: Check game JavaScript syntax
        run: node --check game.js

      - name: Check mobile answer JavaScript syntax
        run: node --check mobile-native-answer.js

      - name: Check patch formatting
        run: git diff --check

      - name: Verify character scaling
        run: grep -q 'mobileCharacterScale' game.js

      - name: Verify stuck recovery
        run: grep -q 'enemy.stuckTime' game.js

      - name: Verify mobile chase mode
        run: grep -q 'const scatterWindow = !MOBILE_RUNTIME.coarse' game.js

      - name: Verify obsolete camera runtime removal
        run: test "$(grep -c 'mobile-camera-fit-runtime.js' index.html || true)" -eq 0

      - name: Commit and push to PR branch
        run: |
          if git diff --quiet; then
            echo "No source changes were required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add game.js index.html
          git commit -m "fix: restore mobile enemy chasing and enlarge characters"
          git pull --rebase origin "${{ github.head_ref }}"
          git push origin "HEAD:${{ github.head_ref }}"
